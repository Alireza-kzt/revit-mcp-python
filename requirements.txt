google-adk>=1.0.0
fastmcp>=2.0.0
pytest
pytest-asyncio
mkdocs
mkdocs-material
google-generativeai # Added as ADK LlmAgent often uses it by default
python-dotenv # For managing environment variables like API keys or server URLs
autodesk-revit-api-stubs # For type hinting Revit API if available, won't be executable outside Revit
# Note: Specific versions might be needed based on compatibility, using general ones for now.
